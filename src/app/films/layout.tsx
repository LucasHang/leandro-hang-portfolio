import { Layout } from '@/components/layout/layout';

export default function FilmsLayout({ children }: { children: React.ReactNode }) {
    return <Layout title="Comercial, Curta-Metragem e Videoclipe">{children}</Layout>;
}
