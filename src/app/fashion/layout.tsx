import { Layout } from '@/components/layout/layout';

export default function FashionLayout({ children }: { children: React.ReactNode }) {
    return <Layout title="Fashion Films">{children}</Layout>;
}
