import { Layout } from '@/components/layout/layout';

export default function ComercialLayout({ children }: { children: React.ReactNode }) {
    return <Layout title="Comercial">{children}</Layout>;
}
