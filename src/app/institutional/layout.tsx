import { Layout } from '@/components/layout/layout';

export default function InstitutionalLayout({ children }: { children: React.ReactNode }) {
    return <Layout title="Institucional">{children}</Layout>;
}
